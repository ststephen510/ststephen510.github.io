XAI_API_KEY=your_api_key_here
XAI_MODEL=grok-4-1-fast-reasoning

# Agent Tools API Configuration
# The Agent Tools API automatically uses web_search capabilities
# No additional search configuration needed - the model handles it autonomously

# Enable debug response logging (default: false)
XAI_DEBUG_RESPONSE=false
