XAI_API_KEY=your_api_key_here
XAI_MODEL=grok-4-1-fast-reasoning

# Live Search Configuration (optional)
# Mode: "auto" (default), "on" (force enable), "off" (disable)
XAI_SEARCH_MODE=auto
# Maximum search results to use (default: 10)
XAI_MAX_SEARCH_RESULTS=10
# Return citations in API response (default: true)
XAI_RETURN_CITATIONS=true
