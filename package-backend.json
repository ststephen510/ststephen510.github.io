{
  "name": "arcticulab-backend",
  "version": "1.0.0",
  "description": "Backend API for arcticulab.io job search",
  "main": "api/search-jobs.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
